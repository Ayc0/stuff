import View from '%/components/View';
import Extensions from '%/components/Extensions';

export const name = 'Ayc0 - VS Code extensions';

const view = () => <View name={name} component={Extensions} />;

export default view;
